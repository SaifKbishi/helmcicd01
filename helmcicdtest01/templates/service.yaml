apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmcicdtest01.fullname" . }}
  labels:
    {{- include "helmcicdtest01.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmcicdtest01.selectorLabels" . | nindent 4 }}
